# apps/web

Placeholder for the Next.js user interface.

Purpose:
- Provide a web UI for safe Gmail cleanup flows.
- Call backend APIs in `apps/api`.
- Keep UI concerns separate from cleanup business logic.
# apps/api

Placeholder for the FastAPI service layer.

Purpose:
- Expose HTTP endpoints for Gmail cleanup workflows.
- Orchestrate request/response concerns and auth boundaries.
- Call shared logic from `packages/core` and existing `gmail_cleanup` modules during migration.